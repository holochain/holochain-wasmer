[package]
name = "host"
version = "0.0.1"
authors = [ "thedavidmeister", "thedavidmeister@gmail.com" ]
edition = "2018"

[dependencies]
wasmer-runtime = "=0.13.1"
common = { path = "../common" }

[lib]
name = "host"
crate-type = [ "cdylib", "rlib" ]
path = "src/host.rs"
